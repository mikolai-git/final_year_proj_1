from setuptools import setup, find_packages

setup(
    name="final_year_proj_1",
    version="0.1.0",
    author="Mikolai Spocinski",
    description="First go at a video to frame exporter and visualiser",
    packages=find_packages(),
    install_requires=[

    ]
)
